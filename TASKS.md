# Tasks

## Phase 1 — Figma assets [x]
- [x] Pull `26:17307` (Card Back) from Figma — verified pixel-for-pixel against the existing `assets/card-back.svg`; already correct, not re-exported.
- [x] Pull `39:396` (Card Back hover, 2 variants) from Figma — both variants are just a rarity-colored border/glow around the same card back, confirming the existing CSS `box-shadow` + `--rarity-color` approach (already in `style.css`) is the right implementation. No new image assets needed.
- [x] Pull `40:407` (Booster Pack) from Figma, exported as `assets/booster-pack.png` (352×528, matches the Figma frame). Not yet wired into the `.booster`/`.booster-face` placeholder — that swap belongs to Phase 2 (booster click + deal animation), left untouched here.
- [x] Cross-check `8:14`/`37:7` against existing `assets/card-front-*` — found and fixed the occlusion-patch issue:
  - Badge already showed `rarity` and the subtitle line already showed `quality` — that part was correct, no fix needed.
  - No `z-index` mixed with `translateZ`/`preserve-3d` in the tilt context — none found.
  - Found `badgeOcclude`/`subtitleOcclude` (blurred image patches hiding wrong baked text) — this violates the "never patch/occlude baked-in text" constraint. Removed them from `app.js`/`style.css`, and deleted the now-dead `badge-blur-*.png`/`subtitle-blur-*.png` files.
  - Tried to re-export `card-front-{ak,de,awp}.svg` from Figma to drop the conflicting baked badge/label text at the source. Blocked: the live Figma file's crack-pattern geometry for `8:14` has drifted from what's baked into the currently-committed SVGs (confirmed by direct pixel comparison — a re-export introduces a visible seam), so the SVGs were left as-is, matching CLAUDE.md's "don't re-export" note.
  - Instead fixed it at the CSS layer: `.badge-text` and `.subtitle-text` now have solid opaque background chips sized to fully cover the conflicting baked text underneath (same pattern the badge pill already used). Verified in-browser on all 3 cards — no bleed-through.
  - Follow-up (not done here, bigger than Phase 1): a designer-provided re-export of the front card art with the badge/label regions genuinely excluded, or promoting weapon-name/skin-name to live DOM against a text-free background.
- [x] Commit: "Phase 1: export card back, hover variants, and booster assets from Figma"

## Phase 2 — Booster click + deal animation [x]
- [x] Booster placeholder → click → burst transition — was already implemented in `app.js`/`style.css` (ported alongside the front-card component before Phase 1 started).
- [x] 3 fixed cards deal out face-down, staggered pop-in — same as above, verified in-browser.
- [x] Wired the real Figma-exported `assets/booster-pack.png` ("GOLDEN KNIVES" art, baked-in "CLICK TO OPEN") into `.booster-face`, replacing the generic gray placeholder + duplicate text label that were still in place. Resized `.booster` to the export's native 352×528 (2:3) aspect. Verified burst + deal still trigger correctly after the swap.
- [x] Commit: "Phase 2: booster open + deal animation"

## Phase 3 — Flip + hover glow [x]
- [x] Hover on face-down card → glow in its rarity color — verified in-browser (purple for the two Classified cards, red for the Covert AWP).
- [x] Click → flip to existing front-card component (reuse, don't rebuild) — verified in-browser, front face renders correctly with badge/subtitle/QR.
- [x] Commit: "Phase 3: flip mechanics + hover glow"

## Revision — Card front re-export (Figma has drifted from committed SVGs) [x]
- [x] Step 1: Diagnose root cause of the seam from the earlier failed re-export attempt (structural analysis, no code changes) — full findings in [`docs/seam-diagnosis-8-15.md`](docs/seam-diagnosis-8-15.md). Short version: the crack texture is a shared `992×480` sheet cropped per-card (this card's crop offset is `x=-328` relative to its own `336×480` frame), so a re-export has to reproduce that exact crop registration or every dependent CSS position (badge/subtitle chips, gun slot, holo mask) drifts out of alignment. Compounding factors: `get_design_context`'s bounding boxes and `get_metadata`'s raw geometry disagree by a consistent 4px (effect-inclusive vs. geometric bounds) on the border-art layer; the committed SVG's two embedded raster images are already broken stubs (empty `href`) from an earlier hand-assembled export, so old and new assets can't be spliced; and the `Union` glow shape's blur effect doesn't survive `get_design_context`'s Tailwind conversion, so it must be verified against raw node data, not the generated reference code.
- [x] Step 2: Document full list of actual differences between old committed SVGs and current Figma design (structure, not just texture) — full per-card comparison in [`docs/card-reexport-differences.md`](docs/card-reexport-differences.md), covering all 3 front SVGs against Figma nodes `8:15`/`25:16671`/`25:15674`. Highlights beyond the Step 1 seam cause: the `992×480` texture-sheet crop offset (`x=-328`) is confirmed identical across all 3 cards, so the Step 3 fix should generalize; AWP's glow (`Union`) is a `boolean-operation` of ~130 sub-vectors vs. a single vector for AK/DE, and uses a different (non-aspect-locked) Product Image sizing mode — highest-risk card for re-export fidelity; all 3 committed SVGs bake their rarity badge pill at a fixed `77px` width regardless of actual rarity text (drifts `12px` off live Figma for AK/DE, `28px` for AWP) — harmless today since the live CSS badge chip covers it, but a template-copy artifact, not Figma drift; AK's SVG alone bakes a redundant, fully-occluded QR-code raster patch that DE/AWP don't have. Also corrected a Step 1 error: the committed SVGs *do* include the 4px rarity-color border stroke, contrary to what Step 1 said (a grep miss, now fixed in that doc).
- [x] Step 3: Trial re-export of AK-47 only, confirm no seam — **clean, no regressions found.** Took a surgical-patch approach rather than a full rebuild: re-fetched `8:15` from Figma, deleted the ~130 flattened `<path fill="#505050" mix-blend-mode:multiply">` shapes that had baked in the mis-registered crop (Step 1's root cause), and replaced them with one `<image>` of the current Figma `Textures` export (downloaded, committed as `assets/card-front-ak-texture.jpg`, ~1.9MB — sizeable next to the rest of `assets/`, flagged as a follow-up for compression, not blocking), positioned at the verified `x=-328,y=0,w=992,h=480` crop registration with `mix-blend-mode:screen` (the actual live value — the old paths used `multiply`, which was itself part of the mismatch). Left everything else in the file byte-identical: title/skin-name/badge-pill paths, gradients, drop-shadow filters, and the `4px` border `stroke` (confirmed still present, matching Step 2's correction) untouched. Removed the dead baked QR-pattern (`pattern0_8_15`/`image0_8_15`, 500×500) after confirming via repo-wide grep that nothing outside the SVG references it — the app's own `.qr-chip` uses a completely separate `qr-code.png` and always fully occluded it. Blur re-check (`filter3_f_8_15`): traced it to a genuine soft radial-gradient glow path (the `Union` shape, previously mis-identified in Step 1 as possibly missing) — left untouched since it's unmodified by this patch, and confirmed via the app's own CSS geometry that this whole region sits fully behind the live `.weapon-slot` gun-image layer in every state, so its fidelity has zero visible effect regardless. Verified in-browser: no visible seam anywhere on the card at 2.2× zoom; badge/subtitle CSS overlays land correctly (unaffected by definition, since their source coordinates weren't touched); tilt-on-hover confirmed via live `transform` inspection; QR click-to-expand and close both confirmed working. (Aside: hit and worked around an unrelated `file://`-preview quirk in the browser tool where `<script src>` didn't execute on first load — confirmed via source diff that it's a preview-tool artifact, not a bug in `app.js` or this change.)
- [x] Step 4: Re-export Desert Eagle and AWP using the same confirmed approach — **clean, no regressions found on either card.** Applied the identical surgical patch from Step 3: re-fetched `25:16671` (DE) and `25:15674` (AWP) from Figma, confirmed both use `mix-blend-mode:screen` and the same `x=-328,y=0,w=992,h=480` crop registration as AK. Downloaded each card's `Textures` export — turned out to be **byte-identical (same SHA1) across all three cards**, confirming Step 1's "shared sheet" theory in the simplest possible form: it's the same fill image duplicated when the AK template was copied to build DE/AWP, not three independently-drifted crops. Deleted each file's ~130 flattened crack-path shapes (lines 4-133, same range as AK) and inserted one `<image>` at the verified offset; left title, badge plaque, gradients, shadow filters, and the `4px` stroke byte-for-byte untouched. Neither DE nor AWP ever had AK's dead baked QR-pattern, so there was nothing to remove there. AWP's two flagged risks from Step 2 checked out fine: the `Union` glow — a `boolean-operation` of ~130 sub-vectors in live Figma — was already flattened to a single radial-gradient path in the committed SVG (same as AK/DE), sitting outside the patched line range, so there was nothing special to handle; the `Product Image` "no aspect-lock" sizing difference turned out to be a Figma-authoring-tool distinction that doesn't survive into the committed SVG at all (it's just a plain rect there, same as AK/DE) — and more to the point, the app never displays that baked SVG product image anyway, since the visible weapon photo is a separate `.gun-img` layer with `background-size:contain` (verified via computed styles) that can't distort regardless. Verified in-browser at 2.2× zoom for both cards: no seam; badge/subtitle CSS overlays correctly positioned (unaffected by definition — their coordinates weren't touched); tilt confirmed via live `transform` inspection on both; QR click-to-expand confirmed on both. AWP-specific: confirmed `holoFoil`/`holoSparkle` layers present with `mix-blend-mode:color-dodge`, and pointer-tracking confirmed live via `--foil-pos-x/y`, `--sparkle-pos-x/y`, `--effect-opacity` custom properties updating on simulated pointer movement — holo shimmer visible in the resulting screenshot, unaffected by the texture patch.
- [x] Step 5: Regression check — tilt, live badge/subtitle text, QR expand, and the AWP holo effect all still work after asset swap — **clean, full end-to-end flow confirmed, nothing to fix.** Tested the complete booster-opening flow on the patched assets in a genuinely fresh browser context (not per-card isolation like Steps 3-4): (1) booster click → deal — all 3 cards fan out cleanly on the new art, no visual glitches; (2) hover glow on face-down cards — confirmed the CSS rule is a baseline glow that *intensifies* on `:hover` (26px/2px → 54px/12px spread), not an on/off toggle, and the intensification is visible in a real (non-synthetic) hover screenshot; (3) click-to-flip on all 3 — front and back faces both render fully, no missing art, no stacking/z-index regression; (4) tilt-on-hover and QR-click-expand confirmed working on all 3 cards via live `transform` inspection and DOM checks (`.qr-overlay` presence), with the expanded QR overlays visually confirmed on all 3 in a full-flow screenshot; (5) AWP holo re-confirmed in the full-flow context specifically (not isolated): `holoFoil`/`holoSparkle` present with `mix-blend-mode:color-dodge` and live pointer-tracking on the AWP card, while explicitly checking AK/DE have *no* holo layers in the same flow (`otherCardsNoHolo: [false, false]`) — matches the Classified/Covert-only design intent; (6) confirmed via `git log`/`git diff` that `assets/booster-pack.png` has no changes since its Phase 1 commit (`a44b675`) — untouched by any re-export work. One testing-methodology note, not an app bug: the sandboxed browser preview's `file://` handling is unreliable for `<script src>` execution and page state persistence across navigations (documented in Step 3 too) — worked around by hopping through an external origin first to force a genuinely fresh tab context, then manually re-initializing `app.js` via `fetch`+`eval`. This occasionally produced a doubled DOM (two independent app instances both reacting to one click) purely as a test-harness artifact; cleaned up mid-test and re-verified. Does not affect the real app, which loads `app.js` normally via a plain `<script src>` tag with no such duplication risk.
- [x] Step 6: Commit and push — commit made; push left to the user per standing policy of not pushing without being asked.

**Card re-export track complete.** All 3 front SVGs (`card-front-{ak,de,awp}.svg`) now source their crack-texture background from a correctly-registered live Figma export instead of the mis-cropped, hand-assembled geometry that caused the original seam.

## Phase 4 — Holo effect (Covert only) [x]
- [x] Pointer-tracking foil + sparkle layers on AWP | Printstream only — present in `app.js` (`holoFoil`/`holoSparkle`, `updateHolo`), masked to the card's crack pattern via `card-mask.png`. Confirmed only the `holo: true` card (AWP) gets the layers.
- [x] Commit: "Phase 4: pointer-tracking holo effect"

## Phase 5 — Polish / open follow-ups [ ]
- [ ] Designer-provided re-export of `card-front-{ak,de,awp}.svg` with badge/label regions genuinely excluded (currently patched with opaque CSS chips per Phase 1 notes), or promote weapon/skin name fully to live DOM.
- [ ] Confirm holo shimmer is visually convincing against the dark Covert card background (subtle in a static screenshot — re-check with live pointer movement).

## Revision — Card back re-sync with Figma (mirroring front re-export process) [x]
- [x] Step B1: Diagnose whether committed card-back.svg differs from current Figma node 26:17307 — structural comparison (color values, texture source, mask/clip structure), not just visual glance — **real differences found, confirmed via direct source values, not a visual/color-profile illusion.** (1) Background color has genuinely shifted: committed bakes `#222B20` (measurably green-tinted — G is the highest RGB channel), live Figma's card frame is `#1a1a1b` (neutral near-black, all channels within 1 of each other) — a real hex-value difference read straight from `get_design_context`'s markup, matching the user's hypothesis exactly. (2) A whole layer is missing, not just a color: live Figma has a `992×480` `Textures` raster layer at `x=-328` offset with `mix-blend-mode:screen` — the identical shared-sheet mechanism already diagnosed and fixed on the front cards — completely absent from the committed SVG, which instead approximates shading with ~40 flat `#131B11` paths (no blend mode). (3) Live Figma also applies a subtle inset bevel shadow to the card frame that has no equivalent in the committed file. Non-issues ruled out: emblem position/size matches between committed and live (both bottom-right, same proportions); the committed file's `viewBox` coordinate offset `(40,40)` and its `<g id="Card_Front DE">` wrapper are both harmless authoined artifacts — the live Figma node is *itself* named "Card_Front DE" (a duplicated/repurposed front-card frame, confirmed via `get_metadata`), not an export mistake; no stroke/border exists on either side, consistent.
- [x] Step B2: Document findings in docs/ — full write-up in [`docs/card-back-diagnosis-26-17307.md`](docs/card-back-diagnosis-26-17307.md), including a same-structure-as-front-cards comparison table and a prioritized summary of what Step B3 (not started) would need to address: background color swap, the missing `Textures` layer (the biggest gap), and optionally the inset bevel as lowest-priority polish.
- [x] Step B3: Patch/re-export if real differences found (surgical patch approach, same as front cards — not full rebuild) — **applied, all 3 items from the B1-B2 diagnosis.** Sourced from Figma node `46:7051` (clean isolated card-back frame, confirmed matching `26:17307`). (1) Background color: `#222B20` → `#1a1a1b`, a one-line hex swap. (2) Added the missing `Textures` layer as a new `<image>` inserted after the existing `Union` gold path (matching Figma's own paint order — Textures sits on top of both `Subtract` and `Union`), `992×480`, `mix-blend-mode:screen`. Downloaded the asset and found it **byte-identical (same SHA1)** to the already-committed `card-front-ak-texture.jpg` — confirmed the whole booster set (3 fronts + back) shares one master texture sheet — so referenced the existing file instead of committing a 4th duplicate ~1.9MB copy. (3) Added the inset bevel shadow (`inset 2px 2px 4px rgba(255,255,255,0.1), inset 4px 4px 8px rgba(0,0,0,0.6)`) — implemented in CSS/JS rather than baked into the SVG (SVG has no native inset-shadow primitive without a complex filter stack; CSS `box-shadow: inset` is the direct equivalent and matches how the rest of the app already layers polish effects). Left the emblem and border stroke untouched per the task, and left the `Subtract`/`Union` paths themselves untouched too — this fix is purely additive (recolor + one new layer + one new shadow), unlike the front cards' subtractive+additive crack-path replacement, because the diagnosis found the existing dark-shading and gold-pattern paths were structurally fine, just missing the color/texture/bevel on top. **One coordinate gotcha caught before it shipped:** this SVG's `viewBox` is offset by `(40,40)` (unlike the front cards' `0,0`-based ones) — the new Textures `<image>` had to be placed at `x=-288,y=40` (not the front cards' `x=-328,y=0`) to land in the same visual position once that offset is accounted for; caught by checking the existing background rect's own `x=40,y=40` before placing the new layer, not by trial and error. Verified color correctness **by direct pixel sampling** (canvas `getImageData` on the rasterized SVG), not by eye: sampled pixel reads back exactly `rgb(26,26,27)` = `#1a1a1b`, zero deviation — worth noting because the two colors (`#222B20` vs `#1a1a1b`) are visually almost indistinguishable in a screenshot at this luminance (both read as "very dark"), so eyeballing alone would not have been a reliable confirmation here.
- [x] Step B4: Regression check — hover glow variants (39:396), flip animation, anything else that depends on card-back.svg — **clean, nothing broken.** Confirmed the JS-driven hover box-shadow (rarity glow + lift/tilt from the earlier revision work) correctly *combines* with the new inset bevel rather than clobbering it — this required updating `applyBackTilt()` in `app.js` to append the bevel's two shadow layers onto both the hover and rest box-shadow strings (since `box-shadow` is set as a single inline style by JS on every hover/leave, a naive addition would have made the bevel disappear the moment a user first hovered any card). Verified via computed style: 5 shadow layers present on hover (rarity ring, glow, drop-shadow, + 2 inset bevel), 5 present at rest (3 transparent placeholders + 2 real inset bevel) — bevel is visually present in both states. Confirmed both rarity-glow variants visually: Classified (purple, AK-47/Desert Eagle) and Covert (red, AWP) both glow correctly on hover and fade cleanly back to neutral on leave. Confirmed flip animation: all 3 cards flip correctly to their front faces, straight-row layout (48px gaps, no rotation) intact post-flip, no stacking/z-index regression.

  **Correction (post-B3/B4):** the above was wrong on two counts, both caught by the user pointing at side-by-side screenshots, not by anything in the verification above. (1) The `Subtract` shading paths' own fill (`#131B11`) was never checked against current Figma — only the background rect's color was diagnosed in B1. Downloaded Figma's current `Subtract` asset directly and grepped its fill: `#111112`, not `#131B11`. Since these ~40 paths visually dominate the card far more than the base rect, this stale color — not the rect — was the main driver of the "still looks green/wrong" impression. Fixed with a bulk find-replace across all 39 occurrences. (2) The B3 "Textures added" claim was **not actually rendering** — `mix-blend-mode:screen` does not reliably composite when the SVG carrying it is loaded as an external CSS `background-image` in this rendering pipeline. This was masked in B3's own verification because the pixel-sampling method used there (`canvas.drawImage()` on a separately-loaded `Image()`) does not evaluate blend modes either, so it read back the correct flat base color and falsely registered as a pass — a canvas sample matching the expected color is not proof the *visual* composite is correct. Confirmed the rendering gap first via a from-scratch investigation using an independent local HTTP server (bypassing the sandboxed preview's own `file://`-specific artifacts, which briefly produced red herrings — a stale mock/placeholder render was observed on some manually-overridden `background-image` swaps mid-investigation and did not reflect the real bug); confirmed inline DOM SVG *does* composite blend modes correctly, isolating the defect specifically to the external-image-reference path. **Fix:** removed the `<image mix-blend-mode:screen>` from inside `card-back.svg` entirely (dead weight — never rendered), and instead added the texture as its own real DOM layer (`.back-texture-layer`, a plain `<div>` with `background-image` + `mix-blend-mode:screen` sitting on top of `.back-tilt-inner`) — the same pattern the front cards' `.holo-layer` already uses successfully. Re-verified visually (screenshot shows clearly visible marbled grain now, matching the Figma reference) and re-ran the B4 regression checks against the new structure: hover glow + lift + inset bevel still combine correctly, flip still works, all 3 cards render identically. **Open question flagged, not yet investigated:** the front cards' in-SVG `mix-blend-mode:screen` texture (from the earlier "Card re-export" track, Steps 3-4) uses the exact same external-SVG-background-image mechanism now confirmed broken for the back — it was visually verified working at the time via screenshots, but was not re-checked in this session; worth a fresh look before trusting those screenshots over this newer finding.
- [x] Step B5: Commit — "Card back: apply Figma-matched fix (color, texture, shadow)"

## Final confirmation — front cards against full group composition [ ]
- [x] Screenshot all 3 fronts together, compare against Figma node 8-14 group view (Steps 3-4 only verified AK individually against 8-15, and DE/AWP individually — never all 3 together against the actual group composition)
- [x] Note outcome, commit if any fix needed — **mostly clean, one precise issue found, not fixed here per instructions.** Compared a fresh in-app screenshot of all 3 currently-rendered front cards (fanned layout) against the Figma `8:14` group screenshot. Matching: card order and rarity colors (purple/purple/red), title/skin-name/"Restricted Claim"/"FACTORY NEW" text styling and position, QR chip position, card aspect ratio (`336:480` native ≈ `400:571` CSS, both ≈0.70), and badge proportions (now compact/content-hugging, consistent with the Figma reference's small pill badges — a result of the in-progress, uncommitted Phase 2/3 badge-sizing fix). Figma's `8:14` reference itself lays the 3 cards out side-by-side with no overlap/rotation — our app's fanned, rotated, overlapping layout is an intentional design difference (interactive deal animation vs. static spec view), not a defect, and isn't treated as one here. One real, precisely-measured issue: in the current fan geometry (`fan.x = ±340px`), measuring actual rendered `getBoundingClientRect()`s, Desert Eagle's (center card) rarity badge sits at `770-863px` while AWP (right card, painted on top per DOM order) starts at `760px` — Desert Eagle's *entire* badge falls behind AWP's card and is visually covered. AK-47's badge (`371-468px`) clears Desert Eagle's left edge (`475px`) by only `7px` — technically not overlapping, but close enough to read as touching at a glance. The overlap is asymmetric (AWP's `+12deg` rotation vs. AK's `-12deg` rotation makes the two overlap zones behave differently at badge height). Title/skin-name text itself (the original spacing-fix concern) is fully clear on all 3 cards — this is a *new*, narrower badge-specific overlap, not a recurrence of the old text-covering issue. Not fixed in this step per the task's explicit instructions — flagging for whoever picks up the in-progress spacing/badge revision work.

  **Resolved (superseded):** the fanned layout that caused this was replaced with a straight-row layout (see "Layout — fanned → straight row" below) — 0deg rotation, 48px fixed gaps, no overlap possible. Confirmed via `getBoundingClientRect()`: badge-to-neighbor clearance is now 64px on both sides, in both face-down and face-up states.

## Layout — fanned → straight row, 48px gap [x]
- [x] Replaced the fan (`rotate(±12deg)`, `±340px` offset, overlapping) with a straight horizontal row: all 3 cards at `rotate(0deg)`, center-to-center spacing `448px` (= `400px` card width + `48px` gap), same `y` for all three. Applies to both face-down and face-up — flipping happens in place, doesn't move the card. Stage C (deal-out from the stacked position) now animates to these row coordinates instead of the old fan coordinates; same staggered timing/easing, only the destination changed. Verified in-browser via `getBoundingClientRect()`: gaps measure exactly `48px` on both sides in both flip states, `rotate(0deg)` confirmed via transform matrix (`a=1,b=0,c=0,d=1`, no shear). Screenshotted both states — straight row, no overlap, no rotation. This removes the Desert Eagle badge-overlap issue entirely (see note above) since no card can ever sit behind another.
- [x] Commit: "Change card layout: fanned → straight row, 48px gap"

## Revision — AWP front card re-sync with Figma (AK-47/Desert Eagle untouched, scoped by user request) [x]
- [x] Diagnosed 4 user-reported defects against live Figma node `25:15674`, confirming root causes structurally (not by eye) before touching any file:
  1. **Badge duplication** — `card-front-awp.svg` still baked a legacy fixed-width (`77px`) badge pill + "Covert" glyphs (`<g filter="url(#filter1_d_25_15674)">`) directly into the art layer, left over from before the badge/subtitle-must-be-live-DOM constraint was enforced. `app.js` *also* renders a live `.badge-text` div on top (per CLAUDE.md's hard constraint) — with both present, they visually overlapped at slightly different bounds, reading as "duplicated". Confirmed via grep that AK-47/Desert Eagle's SVGs have **no** equivalent baked badge — this was AWP-specific leftover cruft, not a shared pattern.
  2. **"Shifted" text** — same root cause, second instance: the SVG also baked a duplicate "FACTORY NEW" text path (`fill="#E34747"`, positioned at the live `.subtitle-text` div's exact location) that the live subtitle chip was rendering on top of/against, reading as misaligned/doubled text.
  3. **Missing QR match** — confirmed via `get_design_context` that Figma's AWP `QR Code` node uses a distinct exported image, while the app hard-coded the same shared `assets/qr-code.png` for all 3 cards.
  4. **Missing BG Layers + Textures** — confirmed via `get_metadata`/`get_design_context` that Figma's AWP frame has a `BG Layers` group (4 `Subtract` shapes, colors `#505050`/`#3B3B3B`/`#A9A9A9`, `mix-blend-mode:multiply` against the card's red base) that is **completely absent** from the committed SVG (not just non-rendering — zero matching elements via grep), plus the same in-SVG `Textures` `mix-blend-mode:screen` layer already confirmed broken for the card back (external-SVG-background-image blend-mode limitation, see the card-back revision above) — meaning the front-card "Card re-export" track's Step 3/4 fix likely never actually rendered either, despite passing its own (canvas-sampling-based, blend-mode-blind) verification at the time. This also strongly suggests the ~130 flattened paths deleted in that earlier work were the *real* `BG Layers` content, mistaken for a mis-registered duplicate of the texture sheet — a probable error in that earlier fix, not re-litigated here since the user scoped this round to AWP only.
- [x] Fix, applied only to `assets/card-front-awp.svg` / AWP's entry in `app.js` / new AWP-only assets — AK-47 and Desert Eagle files untouched (confirmed via `git status`/`git diff` before commit):
  - Removed the baked badge group and the baked duplicate "FACTORY NEW" path from `card-front-awp.svg`, along with the base `#E34747` rect and the broken in-SVG `Textures` `<image>` (both replaced below by real DOM layers, same proven pattern as the card-back fix).
  - Downloaded Figma's `BG Layers` export (node `25:15675`) via `download_assets`; the raw export came back oversized (a whole-page-coordinate artifact, `246KB`) so it was trimmed to just the 3 relevant `Subtract` groups (`#505050`/`#3B3B3B`/`#A9A9A9`) into a clean `336×480` SVG, `assets/card-front-awp-bg-layers.svg` (excluded a 4th `Subtract` sub-group of ~34 individual radial-gradient highlight dots — decorative, not blend-dependent, not part of the user's 3-color report; can be added later if flagged).
  - Downloaded Figma's actual QR export (node `26:16920`) as `assets/card-front-awp-qr.png`.
  - Reused the existing `card-front-awp-texture.jpg` (already committed from the earlier front-card re-export track) for the `Textures` layer, since the crop math (`992×480` sheet, `295.238%`/`center center`) is identical to what already works for the card back.
  - New CSS (`style.css`): `.art-base-layer` (flat `var(--rarity-color)` fill — replaces the SVG's own base rect so the layers under it aren't fully occluded), `.bg-layers-layer` (`mix-blend-mode:multiply`), `.front-texture-layer` (`mix-blend-mode:screen`, same 295.238%/center-center math as `.back-texture-layer`) — all as real DOM `<div>`s, not baked into the referenced SVG, per the now-confirmed external-SVG-blend-mode limitation.
  - `app.js`: `buildFrontFace` now inserts `art-base-layer` → (`bg-layers-layer` if `card.bgLayers` set) → (`front-texture-layer` if `card.texture` set) → `art-layer`, in that DOM order (bottom to top), before the existing holo/weapon/badge/subtitle/QR layers. Added `bgLayers`/`texture`/`qr` fields only to the AWP entry in `CARDS`; AK-47/Desert Eagle entries unchanged, so they get no new layers and keep rendering exactly as before (confirmed — see regression check below). `qrChip`/`qrOverlay` `<img>` sources now read `card.qr || './assets/qr-code.png'`, falling back to the shared file for cards without a specific one.
- [x] Verified in-browser via the established local-HTTP-server method (sandboxed `file://` preview is unreliable for this project, per earlier findings): all 4 reported defects resolved on AWP — single clean "Covert" badge, single "FACTORY NEW" line, visibly cracked/shaded background (multiply-tinted BG Layers + screen-blended Textures both compositing correctly), distinct QR pattern. All 5 new/changed AWP assets returned `200 OK` over the network, zero console errors. Regression-checked AK-47 and Desert Eagle by flipping both in the same session — pixel-identical to their pre-change appearance (purple gradient, single badge/subtitle, no BG-layers/texture, since those cards' `CARDS` entries and SVGs were not touched).
- [ ] Commit — pending (not yet requested by the user this round).

  **Correction (round 2):** user flagged, via an annotated screenshot, that the badge still read as oversized, "Printstream"/"FACTORY NEW" looked misaligned, and "FACTORY NEW" sat on a visible dark rectangle. Root cause: `.badge-text` (`min-width:92px`) and `.subtitle-text` (`background:#0e0812`) were deliberately oversized/opaque per the original Phase 1 fix specifically to *occlude* the baked duplicate badge/quality text still present in each card's own art layer (see Phase 1 note above) — a fix that's still correctly needed for AK-47/Desert Eagle (their SVGs still carry those baked duplicates, untouched by design), but became dead weight once this round's fix removed AWP's baked duplicates: the oversized/opaque styling had nothing left to hide on AWP, so it just showed up as an oversized badge and a stray dark box. Fixed by splitting the occlusion-specific styling into modifier classes (`.badge-text.occlude-wide`, `.subtitle-text.occlude-chip`) applied conditionally in `app.js` based on a new `textClean: true` flag set only on AWP's `CARDS` entry — AK-47/Desert Eagle get no flag, so they resolve to the exact same combined CSS as before (confirmed via screenshot: badge/chip sizing pixel-unchanged on AK-47 after the refactor). AWP now renders the badge at its natural content-hugging width and "FACTORY NEW" as plain colored text with no background, matching Figma. Also answered the user's 4th question (suspected baked-image text) directly rather than silently changing scope: confirmed "AWP"/"Printstream"/"Restricted Claim" are indeed baked vector-outlined paths, not live text — this is the existing project-wide convention on all 3 cards (no `<text>` element anywhere in any committed front SVG), not something specific to or introduced by this AWP fix. Only the rarity badge and quality line are required to be live DOM per CLAUDE.md, and both already are.

## Revision — AK-47 + Desert Eagle brought to parity with AWP, all baked text converted to live DOM [x]
- [x] User asked explicitly to (1) apply the same BG-Layers/Textures/badge/quality fix already done for AWP to AK-47 (`8:15`) and Desert Eagle (`25:16671`), and (2) fix the "texts that are actually images" project-wide, not just badge/quality — a deliberate scope expansion past CLAUDE.md's minimum (only badge=rarity and subtitle=quality are required live), applied here to all three cards for consistency rather than leaving AWP's title/skin-name/sublabel as the odd ones out.
- [x] Pulled `get_design_context`/`get_metadata` for both nodes — confirmed identical template structure to AWP (`BG Layers` group with 4 `Subtract` shapes, `Textures` `mix-blend-mode:screen` layer, `Badge Container` auto-width pill, `Product Details` with Product Name/SubLabel/Label/QR Code, same relative positions). Both use `#C92ADB` (Classified) for base/badge.
- [x] Downloaded and cleaned `BG Layers` exports for both (`download_assets` on `17:15263` for AK, `25:16672` for DE) using the same trim-to-3-Subtract-groups process established for AWP — `assets/card-front-ak-bg-layers.svg`, `assets/card-front-de-bg-layers.svg`. Downloaded each card's real QR export (`26:17296` AK, `26:17294` DE) — `assets/card-front-ak-qr.png`, `assets/card-front-de-qr.png`.
- [x] Edited `card-front-ak.svg` / `card-front-de.svg`: removed the same categories of baked content already stripped from AWP — base rect, broken in-SVG `Textures` `<image>`, baked badge pill+glyphs, baked duplicate quality text — confirmed via grep before deleting that both files had the identical byte-for-byte template structure as AWP's pre-fix state (same `filter0`/`filter1`/`filter2` group layout, same line offsets), so the same line-range surgical delete was safe to apply directly rather than re-diagnosing from scratch.
- [x] Went further than the AWP round per the user's explicit second request: also removed the baked title (weapon name), baked skin-name, and baked "Restricted Claim" sublabel paths from **all three** SVGs (including retroactively from `card-front-awp.svg`, which still had them from before) — these were never required to be live by CLAUDE.md, but the user asked directly for "image texts" to become "real texts", and leaving AWP inconsistent with the newly-fixed AK/DE would have been a worse end state than matching all three.
- [x] `app.js`: added `title`/`skinName` fields to all three `CARDS` entries, plus `bgLayers`/`texture`/`qr` for AK-47/Desert Eagle (reusing the already-committed `card-front-{ak,de}-texture.jpg` from the earlier "Card re-export" track). Added a shared `SUBLABEL = 'Restricted Claim'` constant (identical across all three per Figma, same pattern as the existing `QUALITY` constant). `buildFrontFace` now creates `title-text`/`skin-name-text`/`sublabel-text` live divs alongside the existing `badge-text`/`subtitle-text`, positioned via new percentage-based CSS classes in `style.css` matching Figma's absolute coordinates (title `left:7.14%,top:5%`; skin-name `top:78.3%`; sublabel `top:93.5%`). No web font was added — these inherit the page's existing `Arial, Helvetica, sans-serif` stack, same as the already-live badge/subtitle text, consistent with the project's no-external-dependency static-prototype constraint.
- [x] Since all three cards are now equally "clean" (no baked duplicates left anywhere), removed the round-2 `textClean`/`occlude-wide`/`occlude-chip` conditional system entirely — it's dead weight now that there's no card left needing the oversized/opaque occlusion variant. `.badge-text`/`.subtitle-text` are back to a single unconditional definition (content-hugging badge, transparent quality line) applied identically to all three cards.
- [x] Verified in-browser via the local-HTTP-server method: all three cards show a single clean rarity badge, cracked/shaded BG-Layers+Textures background in the correct color per rarity (purple ×2, red), and five real live-DOM text lines each (title, skin name, quality, sublabel, badge) rendering crisp — resolving the user's suspicion that blurry vector-path text was being mistaken for missing/broken rendering. All new/changed assets (2× bg-layers SVG, 2× QR PNG, both edited SVGs) returned `200 OK`, zero console errors. No visual regressions on any card compared to the last-known-good AWP screenshot.
- [ ] Commit — pending (not yet requested by the user this round).

  **Correction (round 4):** user flagged, via a cropped screenshot of Desert Eagle, that the live text lines still weren't evenly spaced relative to each other — a large, uneven gap had opened up between "FACTORY NEW" and "Restricted Claim" specifically. Root cause: none of the four new/touched text classes (`.title-text`, `.skin-name-text`, `.sublabel-text`, and the pre-existing `.subtitle-text`) had an explicit `line-height`, so each fell back to the browser's font-dependent default (`normal`, roughly 1.15–1.2× font-size) — for `.skin-name-text` at `font-size:36px` that's a ~43px line box despite Figma's own tight `line-height:24px` for that element, silently padding extra space around the glyph and throwing off the visual gap to its neighbors. `.subtitle-text` additionally still carried `padding: 4px 0 4px 1.2%` left over from when it needed to size an opaque occlusion chip (removed two rounds ago) — dead padding nobody else's text line had, adding an asymmetric offset. Fixed by setting `line-height: 1` on all four classes (glyph-tight box, matching how Figma treats text-node bounds) and recomputing each `top` as an exact fraction of the native `480`-tall canvas from Figma's real absolute pixel positions (`skin-name 376/480=78.33%`, `quality 408/480=85%`, `sublabel 445/480=92.7%`), replacing the earlier hand-rounded guesses. Dropped `.subtitle-text`'s now-pointless padding and switched it from `left:4%/right:4%` to `left:7.14%` to match the other three lines' shared left edge exactly. Verified in-browser on all three cards: "Restricted Claim" now sits a small, consistent gap below "FACTORY NEW" instead of drifting toward the QR code, and all four text lines share one true left edge.

  **Correction (round 5):** user reported all card text looked "blurry" — not a spacing complaint this time, a rendering-crispness one. Diagnosed via computed style rather than guessing: `getComputedStyle(tiltInner).transform` read back a non-identity `matrix3d(...)` with small but real rotation components even with the mouse nowhere near the card, confirming the pre-existing idle "breathing" animation (`rx = sin(t*0.5)*2.5deg`, `ry = cos(t*0.35)*3.5deg`, running continuously whenever not hovered) never rests at a flat identity matrix. Every text element was a child of `.tilt-inner` — inside that same `perspective`/`preserve-3d` 3D context — so all five text lines were permanently subject to a slowly-drifting sub-degree 3D rotation, which breaks pixel-grid alignment and font hinting and reads as soft/blurry even in a static screenshot, regardless of scroll or zoom. This is a browser rasterization characteristic of transformed text, not an asset-quality problem — confirmed by the fact it affected all five text lines equally regardless of font-size or which SVG they came from. First attempt: moved all five text elements out of `.tilt-inner` into a flat `.text-overlay` sibling of `.tilt-perspective` so they'd never inherit any 3D rotation — fixed the blur, verified crisp in-browser.

  **Correction (round 6):** user immediately flagged the side effect of round 5's fix — with text pulled out of the 3D context entirely, it no longer tilted along with the art/weapon during hover, reading as "stuck in place" while the rest of the card moved. Reconciled both requirements (crisp at rest, but tilts together with the card on hover) by fixing the actual root cause instead of routing around it: the blur was never really about text living inside the 3D context — it was that the idle sway kept that context perpetually a few degrees off identity, even at rest, with no path back to a mathematically flat `0deg` state. Moved all five text elements back inside `.tilt-inner` (restoring their `transform: translateZ(20px)` depth-ladder entries) and instead removed the idle sway itself: the `requestAnimationFrame` loop in `buildFrontFace` no longer drives `rx`/`ry` from a sine/cosine wave when not hovering (only the AWP holo shimmer's background-position idle animation remains, which never touched `tiltInner`'s transform and was never the blur source). Added an explicit reset on `pointerleave` — `rx = 0; ry = 0; applyTilt();` — mirroring the pattern `buildBackFace`'s `applyBackTilt` already used, so the card eases back to a true `rotateX(0deg) rotateY(0deg)` identity (verified via computed style: `matrix(1, 0, 0, 1, 0, 0)`) instead of drifting into a sine wave. Net effect: the card is now perfectly flat and text is pixel-crisp whenever the user isn't actively hovering, and during an actual hover the whole `tiltInner` subtree — art, weapon, holo, badge, title, quality, everything — tilts together as one rigid layer (confirmed via inline style inspection: `rotateX(7.7deg) rotateY(-7.7deg)` applied and `title-text`'s `parentElement` is `tilt-inner`), with only the expected, transient softness of active real-time 3D motion — not a permanent at-rest defect. `.text-overlay` was removed as dead code now that it's unused.

  **Correction (round 7):** user asked to specifically cross-check text sizing/line-height/spacing against Figma rather than trust the earlier hand-picked values. Re-pulled the AWP design context and compared every text node's native `font-size`/`line-height` (336px-wide canvas) against the committed CSS (400px-wide card), computing the exact scale factor `400/336 = 1.190476`. Found two real mismatches beyond simple rounding: (1) `.skin-name-text` used `line-height: 1` (i.e. line-height = font-size, a 1:1 ratio) but Figma's actual Product Name node is `font-size:32 / line-height:24` — a *tighter* 0.75 ratio (intentional tight leading on the big display line) — so the box was rendering taller than Figma intends. (2) `.sublabel-text` also used `line-height: 1`, but Figma's SubLabel node is `font-size:10 / line-height:16` — a *looser* 1.6 ratio — so the box was rendering shorter than Figma intends. Both errors pushed the gap to their neighboring lines in the wrong direction simultaneously, compounding the "spacing still feels off" impression from earlier rounds even after the top-percentage fix. `.title-text` and `.subtitle-text` happened to already be correct by coincidence (Figma's ratio is exactly 1:1 for both, so `line-height:1` was accidentally right). Fixed by replacing every hand-picked font-size/line-height pair with the precise Figma-native value × 1.190476: title `28.6px/28.6px`, skin-name `38.1px/28.6px`, quality `19px/19px`, sublabel `11.9px/19px`, badge `14.3px/28.6px`. Also corrected `.badge-text`'s padding from `4px 9px` (vertical+horizontal) to `0 9.5px` (horizontal-only, scaled from Figma's `px-[8px]`) since Figma's Badge Container has no vertical padding at all — its height comes purely from the text's own (scaled) line-height via flex centering, same box model the badge now uses. Verified in-browser: text is proportionally larger and closer to the Figma reference (particularly skin-name and quality, which were previously undersized relative to each other), "Mecha Industries" — the longest skin name — still fits on one line with no clipping at the new larger size (expected, since uniform scaling preserves the text-width-to-card-width ratio that already fit in Figma's own 336px frame), zero console errors.

## Revision — booster placeholder: size fix, stack-under-booster reveal, no premature hover, organic burn animation [x]
- [x] **Size mismatch.** User felt the booster looked smaller than the cards. Confirmed via Figma: `.booster` was hard-coded to the booster-pack asset's *native* Figma size (`352×528`, node `40:408`), while the cards had been deliberately scaled up `400×571` (×1.19 from their own native `336×480`) earlier in the project — the booster never got the same treatment. In Figma the booster is already ~5–10% larger than a single card (`352:336`, `528:480`), so scaling the booster by the *same* 1.19 factor used for cards (rather than to match card size 1:1) preserves that relationship: `352×528 → 419×629`. Applied directly to `.booster` in `style.css`.
- [x] **Stack should already be "under" the booster, staggered like the Figma reference (node 81:460), not popped in after.** Diagnosed the reference screenshot precisely: 3 card-backs offset diagonally by `(0,8)/(4,4)/(8,0)` in native 336-wide coordinates (a slight fanned peek, not a dead-center overlap) — computed and centroid-recentered the scaled equivalent (`±4.8px` diagonal) as `STACK_OFFSETS` in `app.js`, applied via new `--stack-x`/`--stack-y` custom properties consumed by `.card-slot.stacked`'s transform. Verified the exact offsets numerically via `getBoundingClientRect()` (391.2/396.0/400.8 left, stepping precisely ±4.8px), not just by eye. Separately (second ask, same round): cards were being *built* only after the booster finished burning, animating in from `opacity:0/scale:0.7`; user wanted them physically in place beforehand, just hidden behind the booster. Reordered `#tray` before `#booster` in `index.html` (DOM order alone puts the booster on top, no z-index needed) and moved card construction out of the click handler into a `buildStack()` call at script init, applying `.stacked` immediately with no transition — confirmed via `elementFromPoint()` that before any click, the booster (not a card) is what's under the pointer at the stack's center, i.e. full coverage.
- [x] **No hover/tilt/click/QR on cards until they've actually dealt out.** Since cards now exist in the DOM (and are interactive) from page load, added `pointer-events: none` to the base `.card-slot` rule and `pointer-events: auto` only on `.card-slot.dealt`, re-enabling all interaction exactly when a card reaches its final row position. Verified via `getComputedStyle(...).pointerEvents` before/after the `dealt` class lands, plus `elementFromPoint()` confirming clicks during the stacked phase land on the booster, not a card.
- [x] **Redesign the "burn" transition to look like a real burning/dissolving effect**, per a reference video the user provided (`Video Project 1.mp4`) showing Polaroid photos dissolving via glowing, organic, fractal-edged burnt holes spreading inward from scattered ignition points — not the old flash-and-fade. Watched the reference by serving a local copy through a throwaway HTML `<video>` page (native seek was unreliable for this specific file — programmatic `currentTime` assignment silently reverted to `0`; real playback + timed screenshots worked and were used instead) and stepping through it, confirming the effect is a classic noise-threshold "dissolve" transition with a glowing amber edge band. Reported findings and three implementation options to the user; they picked the closest-fidelity one (SVG `feTurbulence`-driven mask, no external deps, fits the project's no-build-step constraint) and explicitly said not to implement until asked.
  - Rebuilt `.booster-face` from a CSS `background-image` span into an inline `<svg>` (index.html) — inlining is required, not optional, per this project's own established rule that dynamic SVG effects (filters, blend modes) silently fail to composite when referenced externally as a CSS `background-image`.
  - Two SVG `<filter>`s share one `feTurbulence` fractal-noise field (`baseFrequency 0.012 0.009`, 4 octaves, fixed seed so both filters generate identical noise): `burnAlphaMain` thresholds it once via an animated `feComponentTransfer`/`feFuncA` (`intercept` sweeping `15 → -15`, `slope 30`) to erode the booster art from fully opaque to fully gone; `burnAlphaRing` thresholds the *same* noise twice at a small fixed offset and takes the `feComposite operator="out"` difference — the thin band between the two cutoffs is exactly the strip of "paper" about to catch, dilated (`feMorphology`) and blurred (`feGaussianBlur`) into a glowing edge, tinted via a `radialGradient` (`emberGradient`, white-hot core → orange → deep red) and composited with `mix-blend-mode:screen` over the eroding art.
  - **Bug caught before shipping, not after:** the first version rendered nothing — the booster appeared to vanish entirely (later realized: what was actually showing through was the card stack now sitting behind it, briefly misread as "the booster is broken and showing a card back"). Root-caused via a visible debug rect using the same filter outside of any mask context: `feColorMatrix type="luminanceToAlpha"` zeroes the RGB channels (forces black), and since an SVG mask's effective value is `alpha × luminance`, a fully-opaque-alpha *black* result reads as fully **hidden**, not shown. Fixed by re-flooding white and stencilling it through the computed alpha (`feFlood` + `feComposite operator="in"`) as the last step of both filters, confirmed via the same debug rect turning solid white at rest.
  - `app.js`: the dissolve is authored as SMIL `<animate begin="indefinite">` elements (`burnAnimMain`, `burnAnimRingA`, `burnAnimRingB`) rather than CSS `@keyframes`, since CSS cannot animate SVG filter-primitive attributes; `openBooster()` calls `.beginElement()` on all three at click time so the burn starts exactly on click rather than on page load. `BURN_MS` raised from `450` to `1400` to match the animations' `dur`, keeping the existing stack-pause/fan-out chain timed correctly off the new, longer burn. The old CSS `filter: brightness/blur` flash and radial-gradient `::after` glow were removed as superseded; `.booster.burning` now only stops further clicks and fades the drop-shadow out over the same 1.4s (via `filter: drop-shadow(...)` transition) so a rectangular shadow doesn't linger after the art has visibly dissolved.
  - **Testing note:** this sandboxed browser environment's page timers run faster than wall-clock (confirmed earlier this session with `STACK_PAUSE_MS` too, and reconfirmed here — a `setTimeout`-driven sequence measured in seconds completed within ~1s of real `wait` time), making screenshot-timed mid-animation capture unreliable. Verified the mechanism is genuinely correct a different way: triggered the same filter on a standalone debug rect via real `beginElement()` + a real wait, which rendered the actual organic black/white noise-threshold pattern mid-sweep (not a static image) — proving the SMIL animation and filter chain both execute correctly frame-to-frame, independent of the timing-capture limitation. Full click-through flow (burn → reveal → deal) completes with zero console errors and the correct final state.
- [ ] Commit — pending (not yet requested by the user this round).

  **Correction (round 2):** user asked for the burn to run a bit longer, the fire color to read more saturated, and flagged that it currently "cuts off sharply" at the end. The third point turned out to be a real bug, not a taste note — diagnosed by re-running the debug-rect method from the first round: with the original sweep (`intercept` animating `15 → -15` at `slope 30`), the math only guarantees full erosion for noise-alpha values up to `(0 - (-15))/30 = 0.5`; any pixel where the noise field's luminance-derived alpha exceeded `0.5` (a large, statistically-expected fraction of a fractal noise field, which spans roughly `0..1`) never fully reached zero, even at the animation's own end. Since `BURN_MS` then force-hides the booster via `booster.hidden = true` — a hard `display:none`, no fade — whatever fraction of the image was still stubbornly visible from that undershoot vanished instantly, reading as an abrupt cut. Confirmed the fix the same way as the original bug: the standalone debug rect now renders solid black (fully transparent) after the animation completes, versus a visibly patchy residual pattern before. Fixed by widening the intercept sweep with margin on both ends — `18 → -34` for the main mask (comfortably covers alpha `0..1` at slope 30 with room for turbulence occasionally exceeding `1`), and the ring's paired thresholds shifted the same way (`18 → -34` / `15.5 → -36.5`, preserving the original `2.5` offset between them). Duration bumped `1.4s → 1.9s` across all three `<animate>` elements, `app.js`'s `BURN_MS` and the `.booster.burning .booster-face` drop-shadow-fade transition both updated to match so the hide and the shadow fade still land exactly when the SVG animation finishes. `emberGradient`'s stops swapped for more saturated hues (less pastel/washed): core `#fff3d6→#fff0a3`, `#ffb24a→#ffa000` (mid-orange), `#ff5a1f→#ff3d00` (red-orange), `#8a1a0a→#8f0000` (deep red), stop positions adjusted slightly (`35/70%→30/62%`) to keep the saturated bands reading clearly rather than blurring together. Verified in-browser: full click-through flow still completes with zero console errors.

## Revision — rare-card (AWP/Covert) back: iridescent hover shimmer + click explosion [x]
- [x] User asked for a "more beautiful" card reveal for the rare card specifically, referencing a screen recording of Hearthstone's legendary-card opening effect, plus asked in the same message for a more complex shimmering hover glow and an "explosion" on click/flip for the rare card. Watched the reference the same way as the earlier burn-animation video (served locally, this environment's timers run the video to completion almost instantly on `.play()`, so used `playbackRate = 0.125` to slow it enough to catch specific frames) — confirmed the sequence: soft idle glow (0–1.5s) → intensifying blazing halo (1.5–2.2s) → a full white-hot flash burst coinciding with the flip (2.2–2.8s) → settle into a steady ambient glow around the revealed card. Reported the breakdown and a CSS/JS-only implementation plan (no new dependencies); user confirmed and clarified the flash should fire immediately on click, not synced to the flip's own rotation.
- [x] Scoped to AWP only via the existing `card.holo` flag (already used for the front-face holo shimmer, so it doubles as "this is the rare card" without introducing a second flag). `buildBackFace(rarityColor, isRare)` in `app.js` now takes a second parameter and returns `{ el, triggerExplosion }` instead of a bare element, so `buildCardSlot` can call `triggerExplosion()` from the click handler before `flipCard()` — the explosion click handler and the flip's rotation both start off the same click, but are otherwise independent (matching "fires immediately", not synced to the 560ms rotation transition).
- [x] **Shimmer** (`.back-shimmer-layer`, CSS-only opacity toggle via `.back-tilt-perspective:hover`, no JS): a spinning `conic-gradient` (red → orange → gold → pink → red) blurred and screen-blended behind the card back.
  - **Bug caught before it shipped as "occasionally bleeds onto the neighboring card":** a rotating rectangle's axis-aligned bounding box balloons up to its own diagonal length at 45°/135°/etc — for this element that's up to ~1.75× its resting size — so with the gradient's `inset`/rotation on the *same* element, `getBoundingClientRect()` (and the visible glow) swelled and shrank on every spin, occasionally reaching far enough to overlap the 48px gap into the next card. Confirmed numerically, not just by eye: sampled the live bounding box mid-hover repeatedly and got wildly different widths (`408px` vs `710px`) depending on the rotation phase at sample time. Fixed by splitting the rotation from the clip: `.back-shimmer-layer` itself is a small, fixed-size, non-rotating `overflow:hidden` box (`inset:-5%`), and only its oversized `::before` pseudo-element (`inset:-50%`, safely covers the box at any angle) carries the `conic-gradient` and the `shimmerSpin` animation. Re-verified the same way — sampled the outer box repeatedly across the rotation cycle and it now holds exactly `440×628px` every time, with a stable ~28px clearance to the neighboring card's edge.
- [x] **Explosion** (`.back-explosion-layer`, one-shot `@keyframes explosionBurst` triggered by adding/removing the `.exploding` class from JS): a radial white→gold→red burst that scales up (`0.6× → 1.2× → 1.7×`) while fading out over 700ms, screen-blended so it only brightens rather than occluding the art underneath. `triggerExplosion()` forces a reflow between removing and re-adding the class so the animation would restart correctly if ever re-triggered (not currently possible since a card can only be opened once, but cheap insurance). Verified the actual burst visually (not just "no errors") by pausing the Web Animations API animation mid-flight (`element.getAnimations()[0].pause(); .currentTime = 100`) on a fresh, unflipped AWP back — confirmed a full bright white-gold flash engulfing the card, matching the reference's flash moment.
- [x] Regression-checked AK-47 and Desert Eagle (no `card.holo`) — both flip and render exactly as before, no shimmer/explosion elements created for them (`isRare` false skips both `if` blocks in `buildBackFace` entirely). Full 3-card flip-through confirmed with zero console errors.
- [ ] Commit — pending (not yet requested by the user this round).

  **Correction (round 2):** deployed to GitHub Pages and user reported an artifact on AWP's *front* face — scattered light specks over the card art. First checked whether it was a stale-cache mismatch between the live SVG and the newly-deployed JS/CSS (diffed the live files against the repo directly via `curl`, byte-for-byte identical except CRLF/LF) and confirmed a fresh incognito-equivalent load of the live URL rendered clean — but the user then showed the artifact reproducing in a normal reload too, so it wasn't purely a client cache issue. Traced it in-code rather than guessing further: `.holo-sparkle` (`sparkle.png`, `mix-blend-mode:color-dodge`, opacity `0.22` even at rest via the idle loop) was still live on AWP's *front* face — a pre-existing effect from the project's earlier "Phase 4 — Holo effect" work, unrelated to anything built this session, but visually easy to mistake for a leftover from the new back-face shimmer. Confirmed via computed style (`sparkleOpacity:"0.22"`, `sparkleBgImage` pointing at `sparkle.png`) before touching anything. User confirmed via an explicit before/after screenshot pair that this whole effect (dots + the accompanying `.holo-foil` rainbow gradient layer, neither of which are visible in their "should be" reference) should be removed entirely, not just adjusted.
  - Removed `holoFoil`/`holoSparkle` element creation, the `updateHolo`/`resetHoloIdleTarget`/`setHoloVars` functions, the idle-loop branch that drove them, and the now-dead `hexToHsl`/`hslToCss`/`rarityGradient` helpers (only ever called to build the foil's gradient) from `app.js`. Removed `.holo-layer`/`.holo-foil`/`.holo-sparkle` from `style.css` and fixed a comment that referenced the now-deleted `.holo-layer` as a pattern example.
  - Left `card.holo` itself untouched — it's now doing double duty as "this is the rare card" for the *new*, still-wanted back-face shimmer/explosion effects (`buildBackFace(rarityColor, card.holo)`), so removing it would have broken those. `card-mask.png`/`sparkle.png` assets left in place (now unreferenced by code, only mentioned in historical docs) rather than deleted, since asset cleanup wasn't asked for.
  - Verified in-browser: AWP front face now matches the user's clean reference exactly, AK-47/Desert Eagle unaffected (never had `card.holo`), zero console errors.
